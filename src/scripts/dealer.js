import { stagger } from './_bind.js';
const btn = [...document.querySelectorAll('.phone .btn')].find(b => /place order/i.test(b.textContent)); const steps = [...document.querySelectorAll('.step')]; const tag = document.querySelector('.step')?.parentElement?.querySelector('.tag.g'); const tiles = [...document.querySelectorAll('[style*="grid-template-columns: repeat(3, minmax(0, 1fr)); gap: 8px; margin-top: auto"] > div')];
const qtys = [...document.querySelectorAll('.qty')];
qtys.forEach(q => { const [minus, val, plus] = q.children; const bump = d => { val.textContent = Math.max(0, parseInt(val.textContent, 10) + d); const tot = document.querySelector('.phone [style*="font-weight: 700; font-size: 12px"] div:last-child'); if (tot) { const sum = [...document.querySelectorAll('.qty')].reduce((a, x, i) => a + parseInt(x.children[1].textContent, 10) * [520, 145, 900][i], 0); tot.textContent = '₹' + Math.round(sum * 0.97).toLocaleString('en-IN'); } }; minus.style.cursor = plus.style.cursor = 'pointer'; minus.addEventListener('click', () => bump(-10)); plus.addEventListener('click', () => bump(10)); });
if (btn) { btn.setAttribute('role', 'button'); btn.style.cursor = 'pointer'; btn.addEventListener('click', e => { e.preventDefault(); if (tag) tag.textContent = 'Received · ' + new Date().toLocaleTimeString('en-IN', { hour: '2-digit', minute: '2-digit', hour12: false }); tiles[0] && (tiles[0].querySelector('div').textContent = '…'); stagger(steps, 550); setTimeout(() => { if (tag) tag.textContent = 'Confirmed · ' + new Date().toLocaleTimeString('en-IN', { hour: '2-digit', minute: '2-digit', hour12: false }); tiles[0] && (tiles[0].querySelector('div').textContent = '38 s'); }, steps.length * 550); }); }
